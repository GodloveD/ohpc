%\iftoggle{isx86}{\clearpage}
% begin_ohpc_run
% ohpc_validation_comment Add hosts to cluster

\begin{lstlisting}[language=bash,keywords={},upquote=true,basicstyle=\footnotesize\ttfamily,]
# first add the nodes with --discoverable so warewulf will identify new mac addresses
[sms](*\#*) wwctl node add ${compute_prefix}[1-${num_computes}] --discoverable --ipaddr ${c_ip[0]}

# next set the default profile for all nodes to contain the image we created
[sms](*\#*) wwctl profile set --yes --container rocky-8.9 "default"

# finally, build the overlays for all the nodes
[sms](*\#*) wwct overlay build
\end{lstlisting}
% end_ohpc_run

\end{lstlisting}

